<UserControl x:Class="ReceiptBrowser.Views.ReceiptBrowserView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:prism="http://prismlibrary.com/"
             xmlns:views="clr-namespace:ReceiptBrowser.Views"
             xmlns:vm="clr-namespace:ReceiptBrowser.ViewModels"
             prism:ViewModelLocator.AutoWireViewModel="True">

    <UserControl.Resources>
        <DataTemplate DataType="{x:Type vm:FilterViewModel}">
            <views:FilterView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:TableViewModel}">
            <views:TableView />
        </DataTemplate>

        <DataTemplate DataType="{x:Type vm:DetailsViewModel}">
            <views:DetailsView />
        </DataTemplate>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ContentControl Grid.Row="0" Content="{Binding FilterVM}" />
        <ContentControl Grid.Row="1" Content="{Binding TableVM}" />
        <ContentControl Grid.Row="2" Content="{Binding DetailsVM}" />
    </Grid>
</UserControl>